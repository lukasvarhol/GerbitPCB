spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=create-drop

# Stub values so @Value injection in SecurityConfig succeeds; actual decoding is mocked via @MockBean
spring.security.oauth2.resourceserver.jwt.issuer-uri=https://test-stub.example.com/
supplier.security.audience=https://gerbitpcb-supplier
